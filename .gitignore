# macOS
.DS_Store

# SwiftPM 构建
.build/

# 打包产物（app 包 / 分发包 / DMG）
VPNStatusBar.app/
dist/
*.dmg

# 内置引擎二进制（build-openvpn.sh 产物，架构相关，不入库；用脚本重建）
vendor/openvpn

# 设计稿（不入库）
design/
design.zip

# 构建用材料（不入库；缺省时 build.sh 会跳过对应环节）
dist-src/

# 内部工具
dsh-img.mjs
vision.mjs

# 临时
*.log
