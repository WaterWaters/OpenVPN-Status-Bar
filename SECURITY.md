# 安全策略

## 支持的版本

| 版本 | 支持状态 |
|---|---|
| main（开发版） | ✅ 支持 |
| 历史 Release | ⚠️ 仅安全修复（如有） |

## 报告安全问题

请**不要**把安全漏洞公开发布在 Issue 或讨论区。

请将安全问题发送邮件至 <whm.1029@gmail.com>，标题注明 `[SECURITY]`，并尽可能包含：

- 漏洞类型与影响范围
- 复现步骤 / PoC
- 受影响的版本
- 建议的修复方向（可选）

我们会尽快（通常 72 小时内）确认并给出处理时间表。

## 安全说明

本项目为内部/个人工具，会请求**一次性系统管理员授权**以创建 VPN 隧道，并写入 `sudoers` 免密规则指向内置的 openvpn 引擎路径：

- 授权仅用于 `/usr/local/vpnstatusbar/openvpn` 这一条精确路径，不开放任意命令。
- 凭据密码仅存 macOS Keychain，不落明文文件；连接时临时生成、断开即删。
- 内置 openvpn 引擎为静态自包含构建，无外部 dylib 依赖。

如需对本项目的权限模型做安全审计，欢迎通过上述邮箱联系。
